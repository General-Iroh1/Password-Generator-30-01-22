Made first screen stay, but the second screen gets deleted, then the new screen appears.
View pass route finished.
Delete pass route finished.

Next:
    Fix create pass route
    Add it to register button
    Fix back buttons.